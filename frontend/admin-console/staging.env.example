# Copy to ".env.staging" in this folder (gitignored):
#   copy staging.env.example .env.staging
# Then set DEV_API_PROXY and run: npm run dev:staging
#
DEV_API_PROXY=
#
# Direct API mode (optional — requires ADMIN_CORS_ORIGINS includes http://localhost:5173):
# VITE_ADMIN_API_ORIGIN=
#
# VITE_ADMIN_DUMMY_DASHBOARD=false

import type { FC } from 'react'
import GameSearchField from './GameSearchField'

/** Prefer `GameSearchField` in `CasinoSearchStrip`; kept for compatibility. */
const HeaderGameSearch: FC = () => <GameSearchField />

export default HeaderGameSearch
